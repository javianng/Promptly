import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Promptly")
                .font(.title)
            Text("Use Command + Shift + P to activate")
                .font(.subheadline)
        }
        .padding()
    }
} 